# v22 - Zero-overhead refined expert placement

v22 keeps the validated v19 TP3/EP3 + KDA W8A16 execution path, but changes how the 288 routed experts are assigned to the three EP ranks.

The goal was to reduce per-layer MoE rank imbalance without adding a decode-time expert-remapping kernel.

```text
v19
-> profile TP3 collectives / MoE skew (v20, profiling only)
-> test runtime logical->physical remap (v21)
-> measure runtime lookup overhead with identity control
-> move the same mapping entirely to load time
-> v22 zero-overhead refined placement
```

**Local image:** `voipmonitor/vllm:glm53-native-tp3-bf16-v22-refined-zero-overhead`

**Recommended public tag when published:** `azallaza/glm53-tp3-testing:v22-refined-zero-overhead`

v20 and v21 were development/profiling images only and do not need separate documentation files.

---

# What v20 and v21 established

## v20 - profiling-only collective / MoE instrumentation

v20 was a child of v19 used to instrument TP/EP collectives and MoE phases. It was not intended as a serving image.

The source-level accounting and Nsight traces established the decode collective structure:

```text
1 input-embedding TP AllReduce
45 attention o_proj TP AllReduce
3 dense-MLP down-projection TP AllReduce
42 sparse-MoE final TP AllReduce
---------------------------------
91 AllReduce / generated token

plus:

1 logits AllGather / generated token
```

The strongest repeatable rank-arrival skew was generated inside the sparse MoE phase rather than by the collective implementation itself. The active NVFP4 backend was FlashInfer CUTLASS with contiguous physical expert ownership:

```text
EP0: physical experts   0..95
EP1: physical experts  96..191
EP2: physical experts 192..287
```

World-size-3 communication remained PyNCCL because the tested alternative all-reduce backends did not support this geometry.

## v21 - runtime remap prototype

v21 tested the refined logical expert placement by adding a cached logical-to-physical lookup in the router during every sparse layer.

Correctness passed, but the production result was effectively flat because the runtime indexed gather consumed the small load-balance gain.

Same 512-token benchmark:

```text
v21 refined runtime remap: ~111.06 tok/s
v21 identity lookup:       ~111.02 tok/s
v19 baseline:              ~111.91 tok/s
```

The identity control showed that the runtime lookup itself cost roughly 0.8% relative to the v19 baseline.

That led directly to v22: keep the refined ownership, but remove the decode-time lookup completely.

---

# v22 design

The checkpoint has:

```text
routed experts:          288
experts selected/token:    8
EP ranks:                  3
physical experts/rank:    96

n_group:                   1
topk_group:                1
topk_method:               noaux_tc
router dtype:              float32
norm_topk_prob:            true
routed_scaling_factor:     2.5
```

Because `n_group=1` and `topk_group=1`, all experts are in the same routing group. A consistent permutation of router output channels, correction bias, and loaded expert weights therefore preserves routing semantics while changing which physical EP rank owns each logical expert.

v22 performs that permutation only while loading the model. There is **no logical-to-physical gather in the decode router**.

---

# Refined ownership

The refined placement was checked on a held-out prompt set covering 21,504 sparse token-layer cases.

| Metric | Linear | Refined |
|---|---:|---:|
| mean busiest rank | 4.0371 | 3.9716 |
| p95 busiest rank | 6 | 5 |
| busiest >= 5 | 24.68% | 21.48% |
| busiest >= 6 | 5.13% | 4.01% |
| ideal 3/3/2 split | 26.64% | 28.69% |
| mean rank spread | 2.6924 | 2.5668 |
| excess assignments | 22,301 | 20,894 |
| maximum busiest rank | 8 | 8 |

Refined versus linear:

```text
mean busiest rank:  -1.62%
>=5 cases:          -3.20 percentage points
>=6 cases:          -1.12 percentage points
ideal split:        +2.05 percentage points
mean rank spread:   -4.67%
excess assignments: -6.31%
p95 busiest rank:    6 -> 5
```

The improvement is deliberately modest. v22's purpose is to keep that gain without paying a per-token remapping tax.

---

# How v22 encodes the placement

Two inverse permutations are used:

```text
L2P = logical expert -> physical expert
P2L = physical expert -> logical expert
```

Physical expert slots remain contiguous to FlashInfer:

```text
physical 0..95     -> EP0
physical 96..191   -> EP1
physical 192..287  -> EP2
```

Only the logical expert content stored in those physical slots changes.

Example:

```text
logical expert 2 -> physical 0   -> EP0
logical expert 1 -> physical 96  -> EP1
logical expert 0 -> physical 192 -> EP2
```

## Router load-time permutation

During `Glm5Next` weight loading, v22 intercepts sparse router tensors after the normal GLM weight-name remap.

For each sparse layer, `.mlp.gate.weight` has shape `[288, 4096]` and is reordered on output/expert dimension 0 using `P2L`.

The router correction bias `.mlp.gate.e_score_correction_bias` has shape `[288]` and is reordered by the same `P2L` permutation.

Conceptually:

```python
loaded_weight = loaded_weight.index_select(0, P2L)
```

The router therefore emits the **physical** expert IDs directly. No runtime mapping is required.

## Routed-expert weight loading

The routed-expert loader preserves the original logical expert ID for checkpoint semantics and ModelOpt scale handling:

```text
global_expert_id = logical checkpoint expert ID
physical_expert_id = L2P[global_expert_id]
```

The physical ID is then passed through the normal contiguous EP-local mapping, while the original logical `global_expert_id` is retained for ModelOpt global-scale indexing.

---

# Image lineage / build process

v22 is a small child image of the stable v19 image:

```text
voipmonitor/vllm:glm53-native-tp3-bf16-v19-kda-w8a16
                         |
                         +-- expert_permutation.json
                         +-- patch_v22.py
                         |
                         v
voipmonitor/vllm:glm53-native-tp3-bf16-v22-refined-zero-overhead
```

Development build context:

```text
~/glm53-v22-zero-overhead
```

The static permutation data came from:

```text
~/glm53-v21-refined-placement/expert_permutation.json
```

Only the mapping data is reused. v21's runtime router-gather implementation is not inherited.

Minimal child-image layout:

```dockerfile
FROM voipmonitor/vllm:glm53-native-tp3-bf16-v19-kda-w8a16

COPY expert_permutation.json /tmp/expert_permutation.json
COPY patch_v22.py /tmp/patch_v22.py

RUN python3 /tmp/patch_v22.py
```

The patch modifies only the installed GLM/vLLM source required to reorder router tensors at load and place routed-expert weights into the refined physical slots.

Verification marker:

```text
GLM53_V22_ZERO_OVERHEAD_REFINED_PLACEMENT
```

The production image has no v21 BaseRouter remap helper and no runtime expert-placement gather.

---

# What remains unchanged from v19

```text
checkpoint:                  LibertAIDAI/GLM-5.3-Flash-NVFP4
TP / EP:                     3 / 3
context:                     1,048,576
routed experts:              288 global / 96 physical per GPU
shared expert:               BF16, TP3-sharded
KDA logical heads:           64 -> padded 66 -> 22/GPU
MLA logical heads:           64 -> padded 72 -> 24/GPU
KV cache:                    FP8
KDA decode backend:          Triton
KDA in_proj_qkvgfab:         FP8 E4M3 weight / BF16 activation
KDA projection kernel:       Humming W8A16
MLA backend:                 B12X
MLA KV format:               fp8_ds_mla
NVFP4 MoE backend:           FLASHINFER_CUTLASS
FlashInfer autotune:         enabled
custom all-reduce:           disabled
TP/EP all-reduce:            PyNCCL / NCCL 2.31.2
CUDA graph mode requested:   FULL
effective graph mode:        FULL_DECODE_ONLY
vision (text baseline):      disabled via --language-model-only
vision (validated add-on):     enabled with --mm-encoder-tp-mode data
MTP / DFlash:                disabled
```

The on-disk LibertAIDAI checkpoint remains untouched.

---

# Baseline compose - latency / maximum KV capacity

This conservative configuration was used during the original v22 correctness and single-request speed validation:

```yaml
services:
  glm53-native-tp3:
    image: voipmonitor/vllm:glm53-native-tp3-bf16-v22-refined-zero-overhead
    container_name: glm53-native-tp3
    restart: "no"

    ipc: host
    shm_size: "64gb"

    ports:
      - "15015:8000"

    volumes:
      - /m2-2/huggingface:/root/.cache/huggingface

    environment:
      HF_HUB_OFFLINE: "1"

      GLM53_DEBUG_KDA_STATE: "0"
      GLM53_DEBUG_MLA_HEADS: "0"
      GLM53_DEBUG_INDEXER: "0"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1", "2"]
              capabilities: [gpu]

    command:
      - LibertAIDAI/GLM-5.3-Flash-NVFP4

      - --tensor-parallel-size
      - "3"

      - --enable-expert-parallel

      - --language-model-only

      - --max-model-len
      - "1048576"

      - --max-num-seqs
      - "1"

      - --max-num-batched-tokens
      - "2048"

      - --gpu-memory-utilization
      - "0.95"

      - --kv-cache-dtype
      - fp8

      - --disable-custom-all-reduce

      - --served-model-name
      - GLM-5.3-Flash-TP3

      - --moe-backend
      - auto

      - --linear-backend
      - auto

      - --enable-flashinfer-autotune

      - --additional-config
      - '{"glm53_kda_decode_backend":"triton"}'
```

---

# General-purpose compose - higher prefill / concurrency

After correctness was frozen, scheduler limits were raised for higher prefill throughput and concurrent serving:

```yaml
services:
  glm53-native-tp3:
    image: voipmonitor/vllm:glm53-native-tp3-bf16-v22-refined-zero-overhead
    container_name: glm53-native-tp3
    restart: "no"

    ipc: host
    shm_size: "64gb"

    ports:
      - "15015:8000"

    volumes:
      - /m2-2/huggingface:/root/.cache/huggingface

    environment:
      HF_HUB_OFFLINE: "1"

      GLM53_DEBUG_KDA_STATE: "0"
      GLM53_DEBUG_MLA_HEADS: "0"
      GLM53_DEBUG_INDEXER: "0"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1", "2"]
              capabilities: [gpu]

    command:
      - LibertAIDAI/GLM-5.3-Flash-NVFP4

      - --tensor-parallel-size
      - "3"

      - --enable-expert-parallel

      - --language-model-only

      - --max-model-len
      - "1048576"

      - --max-num-seqs
      - "8"

      - --max-num-batched-tokens
      - "8192"

      - --gpu-memory-utilization
      - "0.95"

      - --kv-cache-dtype
      - fp8

      - --disable-custom-all-reduce

      - --served-model-name
      - GLM-5.3-Flash-TP3

      - --moe-backend
      - auto

      - --linear-backend
      - auto

      - --enable-flashinfer-autotune

      - --additional-config
      - '{"glm53_kda_decode_backend":"triton"}'
```

---

# Scheduler / KV-cache trade-off

`max_num_batched_tokens` has a large memory impact on this build because vLLM profiles/reserves peak activation memory for the allowed scheduler batch.

| Metric | seqs=1 / batched=2048 | seqs=8 / batched=8192 |
|---|---:|---:|
| max_num_seqs | 1 | 8 |
| max_num_batched_tokens | 2,048 | 8,192 |
| CUDA graph capture sizes | `[1, 2]` | `[1, 2, 4, 8, 16]` |
| model-load memory / GPU | ~59.5 GiB | 59.95 GiB |
| peak activation / GPU | ~4.7-4.79 GiB | 15.36-16.10 GiB |
| initial graph-memory estimate / GPU | ~1.46-1.48 GiB | 5.47-5.49 GiB |
| actual CUDA graph pool / GPU | ~0.09 GiB | 0.15 GiB |
| available/current KV cache | ~25.1 GiB | ~12.47-13.23 GiB |
| GPU KV-cache capacity | ~4,242,238 tokens | 2,115,127 tokens |
| 1,048,576-token max concurrency | ~4.05x | 2.02x |

Representative `8 / 8192` startup lines:

```text
max_num_batched_tokens=8192
max_num_seqs=8
cudagraph_capture_sizes=[1, 2, 4, 8, 16]
max_cudagraph_capture_size=16

Available KV cache memory: 12.47 GiB
GPU KV cache size: 2,115,127 tokens
Maximum concurrency for 1,048,576 tokens per request: 2.02x
```

Final per-rank memory examples:

```text
GPU0: consumed 61.65 GiB | peak activation 16.10 GiB | graph 0.15 GiB | KV 12.47 GiB
GPU1: consumed 61.67 GiB | peak activation 15.36 GiB | graph 0.15 GiB | KV 13.19 GiB
GPU2: consumed 61.63 GiB | peak activation 15.36 GiB | graph 0.15 GiB | KV 13.23 GiB
```

Lowering `max_num_batched_tokens` can recover a large amount of KV cache because peak activation reservation falls. The trade-off is smaller prefill chunks: fewer prompt tokens can be processed per scheduler step, which can reduce prefill throughput.

The model itself is unchanged by this scheduler choice.

---

# Short decode benchmark

Two five-run sets using the same fixed 512-token prompt produced:

```text
Set 1 average: 112.50 tok/s
Set 2 average: 112.51 tok/s
```

Compared with v19:

```text
v19: ~111.91 tok/s
v22: ~112.51 tok/s

gain: ~+0.60 tok/s
relative: ~+0.53%

v19 token time: ~8.936 ms
v22 token time: ~8.888 ms
saving:          ~0.048 ms/token
```

---

# 4096-token coherency / decode check

| Prompt | Completion | Wall time | Client rate | Finish |
|---|---:|---:|---:|---|
| networking | 4,096 | 35.938 s | 113.98 tok/s | length |
| storage | 4,096 | 36.055 s | 113.61 tok/s | length |
| concurrency | 4,096 | 36.243 s | 113.01 tok/s | length |

All three used the full budget in the `reasoning` field and returned no final `content`, matching the previously observed GLM template/reasoning-parser behavior for this prompt style.

---

# Estonia validation

```text
prompt chars:                 707,372
prompt tokens:                133,186 in scout
runs:                         30
concurrency:                  1
max tokens:                   40,000

completed:                    30/30
score:                        PASS 30 / FAIL 0
hit max_tokens:               0
completion tokens avg:        2,707
completion tokens p50:        2,524
completion tokens p90:        3,696
completion tokens p99:        6,547
elapsed avg:                  25.2 s
TTFT avg:                     0.38 s
aggregate generation speed:   108.9 tok/s
mean per-request speed:       109.0 tok/s

whole-run GPU power avg:      881 W
whole-run GPU power max:      905 W
reported node limit:          900 W
runtime:                      13m 09s
samples:                      341
```

Compared with v19:

```text
v19: 30/30 @ 108.5 tok/s
v22: 30/30 @ 108.9 tok/s
```

The one-shot prefill scout is not used as the primary prefill measurement.

---

# Estonia-long validation

```text
prompt chars:                 708,291
prompt tokens:                133,405 in scout
runs:                         30
concurrency:                  1
max tokens:                   40,000

completed:                    30/30
score:                        PASS 30 / FAIL 0
hit max_tokens:               0
completion tokens avg:        3,248
completion tokens p50:        3,124
completion tokens p90:        5,729
completion tokens p99:        7,036
elapsed avg:                  30.4 s
TTFT avg:                     0.41 s
aggregate generation speed:   108.4 tok/s
mean per-request speed:       108.7 tok/s

whole-run GPU power avg:      895 W
whole-run GPU power max:      919 W
reported node limit:          900 W
runtime:                      15m 10s
samples:                      393
```

Compared with v19:

```text
v19: 30/30 @ 108.0 tok/s
v22: 30/30 @ 108.4 tok/s
```

Combined correctness:

```text
Estonia + Estonia-long: 60 / 60 PASS
max-token hits:          0
```

---

# Repeated prefill benchmark

The publishable prefill numbers use repeated standalone prefill measurements rather than the noisy one-shot scout values from mixed runs.

| Context | Prompt tokens | TTFT | Client prefill tok/s | N |
|---|---:|---:|---:|---:|
| 8k | 8,202 | 0.80 s | 10,250 | 10 |
| 16k | 16,231 | 1.64 s | 9,881 | 5 |
| 32k | 32,324 | 3.32 s | 9,726 | 3 |
| 64k | 64,516 | 6.99 s | 9,235 | 2 |
| 128k | 128,890 | 14.34 s | 8,991 | 1 |

Observed PCIe RX/TX averages:

```text
8k:    29134 / 29277
16k:   28481 / 28013
32k:   35741 / 34193
64k:   30009 / 35784
128k:  32978 / 32868
```

---

# Concurrent decode benchmark

Aggregate generation throughput using the `seqs=8 / batched=8192` configuration:

| Context | C=1 | C=2 | C=4 | C=8 |
|---|---:|---:|---:|---:|
| 0 | 111.4 | 164.7 | 301.7 | 505.7 |
| 8k | 109.8 | 161.6 | 289.3 | 484.4 |
| 16k | 110.0 | 164.1 | 293.8 | 495.0 |
| 32k | 109.3 | 162.2 | 291.4 | 496.2 |
| 64k | 107.9 | 159.2 | `∅ (4/4)*` | 489.2 |
| 128k | 106.5 | 157.6 | 277.8 | 475.6 |
| 256k | 105.0 | 155.6 | 271.9 | 462.8 |
| 512k | 101.6 | 151.5 | 257.6 | `∅` |

The benchmark's status notation is preserved for cells that did not report a throughput value.

Selected points:

```text
C1 @ 0:      111.4 tok/s
C8 @ 0:      505.7 aggregate tok/s
C8 @ 32k:    496.2 aggregate tok/s
C8 @ 128k:   475.6 aggregate tok/s
C8 @ 256k:   462.8 aggregate tok/s
C4 @ 512k:   257.6 aggregate tok/s
C1 @ 512k:   101.6 tok/s
```

---

# Vision validation

Vision was enabled on the same v22 image after the text-only baseline was frozen.

The first attempt simply removed `--language-model-only`. Model construction then failed in the vision attention path:

```text
AssertionError: 16 is not divisible by 3
```

The checkpoint vision configuration uses:

```text
depth:                    24
hidden_size:              1024
num_heads:                16
head_dim:                 64
intermediate_size:        4096
out_hidden_size:          4096
projection_intermediate:  10240
patch_size:               14
spatial_merge_size:       2
temporal_patch_size:      2
```

The native vLLM implementation already supports running the multimodal encoder in data-parallel/replicated mode. Enabling:

```text
--mm-encoder-tp-mode data
```

makes the ViT use its full 16 heads on each worker instead of attempting `16 / TP3`. The language model remains TP3/EP3.

The working vision changes relative to the general-purpose text compose are therefore:

```diff
- --language-model-only
+ --mm-encoder-tp-mode
+ data
```

No v22 source patch was required for vision bring-up.

## Vision-enabled compose

```yaml
services:
  glm53-native-tp3:
    image: voipmonitor/vllm:glm53-native-tp3-bf16-v22-refined-zero-overhead
    container_name: glm53-native-tp3
    restart: "no"

    ipc: host
    shm_size: "64gb"

    ports:
      - "15015:8000"

    volumes:
      - /m2-2/huggingface:/root/.cache/huggingface

    environment:
      HF_HUB_OFFLINE: "1"

      GLM53_DEBUG_KDA_STATE: "0"
      GLM53_DEBUG_MLA_HEADS: "0"
      GLM53_DEBUG_INDEXER: "0"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1", "2"]
              capabilities: [gpu]

    command:
      - LibertAIDAI/GLM-5.3-Flash-NVFP4

      - --tensor-parallel-size
      - "3"

      - --enable-expert-parallel

      - --max-model-len
      - "1048576"

      - --max-num-seqs
      - "8"

      - --max-num-batched-tokens
      - "8192"

      - --gpu-memory-utilization
      - "0.95"

      - --kv-cache-dtype
      - fp8

      - --disable-custom-all-reduce

      - --served-model-name
      - GLM-5.3-Flash-TP3

      - --moe-backend
      - auto

      - --linear-backend
      - auto

      - --enable-flashinfer-autotune

      - --mm-encoder-tp-mode
      - data

      - --additional-config
      - '{"glm53_kda_decode_backend":"triton"}'
```

## Vision startup / memory

Successful startup showed:

```text
mm_encoder_tp_mode:        data
ViT attention backend:     FLASH_ATTN
MMEncoderAttention:        FLASH_ATTN
encoder cache budget:      32,242 tokens
model load memory/GPU:     61.0 GiB
weight load time:          26.17 s
CUDA graph pool/GPU:       0.15 GiB actual
```

Representative final per-rank memory with vision + `8 / 8192`:

| Rank | Consumed | Peak activation | Graph | Current KV |
|---|---:|---:|---:|---:|
| GPU0 | 62.96 GiB | 16.22 GiB | 0.15 GiB | 11.03 GiB |
| GPU1 | 62.91 GiB | 16.93 GiB | 0.15 GiB | 10.38 GiB |
| GPU2 | 62.87 GiB | 16.93 GiB | 0.15 GiB | 10.42 GiB |

The engine reported the resulting KV pool as:

```text
Available KV cache memory (GPU0):      11.03 GiB
GPU KV cache size:                  1,761,607 tokens
Maximum concurrency @ 1,048,576:        1.68x
```

For comparison, the text-only `8 / 8192` baseline provided:

```text
Available KV cache memory (GPU0):      12.47 GiB
GPU KV cache size:                  2,115,127 tokens
Maximum concurrency @ 1,048,576:        2.02x
```

So enabling the replicated vision encoder reduced the engine KV pool by **353,520 tokens (~16.7%)**, from about **2.115M to 1.762M tokens**. On the GPU0 memory figure, available KV fell by **1.44 GiB**, from **12.47 to 11.03 GiB**.

The difference comes from keeping the complete vision encoder on every TP worker. The language model remains TP3/EP3, while `--mm-encoder-tp-mode data` avoids the incompatible `16 / TP3` vision-head split.

## Vision correctness and speed

Two different PNG images were sent through the OpenAI-compatible `/v1/chat/completions` multimodal path with:

```text
temperature:   0
max_tokens:    512
streaming:     enabled
prompt:        "Analyze this image in detail."
```

### `anime_hill.png`

![anime_hill vision test](images/anime_hill.png)

The model correctly identified the anime-style scene, a dark-haired person standing on a grassy hill, clothing, sky, rocks, flowers, composition, and overall atmosphere.

```text
prompt tokens:          1,390
completion tokens:        512
reasoning tokens:         414
total tokens:           1,902

total request time:     4.604 s
TTFT:                   0.142 s
post-first-token:       4.462 s
decode:                 114.75 tok/s
```

### `retro_anime_portrait.png`

![retro_anime_portrait vision test](images/retro_anime_portrait.png)

The model correctly identified the central figure, traditional Arabian clothing, keffiyeh/agal, white robe, green belt, curved sword, floral framing, purple background, and retro-anime composition.

```text
prompt tokens:          1,390
completion tokens:        512
reasoning tokens:         337
total tokens:           1,902

total request time:     4.698 s
TTFT:                   0.234 s
post-first-token:       4.463 s
decode:                 114.71 tok/s
```

Vision decode remained effectively identical across both tests:

```text
anime_hill.png:              114.75 tok/s
retro_anime_portrait.png:    114.71 tok/s
mean:                        114.73 tok/s
```

These are end-to-end multimodal requests. TTFT includes image processing/encoder work plus prompt prefill, while the reported decode rate is measured after the first streamed token.

Because vLLM initializes an encoder cache, repeated requests using the exact same image should not be treated as independent cold-encoder measurements. Different images were used here to avoid relying only on a same-image cache hit.

> The Markdown expects the two test images at `images/anime_hill.png` and `images/retro_anime_portrait.png` in the repository.

---

# v22 headline results

```text
TP / EP:                    3 / 3
context:                    1,048,576
KV dtype:                   FP8
KDA decode:                 Triton
KDA hot projection:         FP8 E4M3 W8A16 / Humming
MoE backend:                auto -> FLASHINFER_CUTLASS
expert placement:           refined, encoded at load time
runtime placement lookup:   none
FlashInfer autotune:        enabled
custom all-reduce:          disabled
MTP / DFlash:               disabled

text-only 512-token decode: ~112.51 tok/s
vs v19:                     ~+0.53%

Estonia:                    PASS 30 / FAIL 0 @ 108.9 tok/s
Estonia-long:               PASS 30 / FAIL 0 @ 108.4 tok/s
combined:                   60 / 60 PASS

prefill:
8k:                         10,250 tok/s
16k:                         9,881 tok/s
32k:                         9,726 tok/s
64k:                         9,235 tok/s
128k:                        8,991 tok/s

aggregate decode:
C1 @ 0:                     111.4 tok/s
C4 @ 0:                     301.7 tok/s
C8 @ 0:                     505.7 tok/s

KV pools:
text 1 seq / 2048 max_batch_token:              4,242,238 tokens / 25.12 GiB / 4.05x @ 1M
text 8 seq / 8192 max_batch_token:              2,115,127 tokens / 12.47 GiB / 2.02x @ 1M
vision 8 seq / 8192 max_batch_token:            1,761,607 tokens / 11.03 GiB / 1.68x @ 1M

vision:
mode:                       replicated MM encoder
flag:                       --mm-encoder-tp-mode data
ViT attention:              FLASH_ATTN
anime_hill TTFT/decode:     0.142 s / 114.75 tok/s
retro portrait TTFT/decode: 0.234 s / 114.71 tok/s
```

---

# Conclusion

v22 is the current validated native TP3/EP3 baseline and now has a working vision configuration without modifying the v22 model source.

It preserves v19's KDA W8A16 improvement and replaces v21's runtime expert remap with a load-time permutation that adds no decode-time mapping kernel.

Two text scheduler profiles are documented intentionally:

```text
1 / 2048 -> more KV capacity, conservative latency/correctness baseline
8 / 8192 -> higher prefill/concurrency headroom, smaller KV pool
```

Vision uses the same `8 / 8192` general-purpose profile with the multimodal encoder switched to data-parallel/replicated mode:

```text
--mm-encoder-tp-mode data
```

The next major work should proceed on top of this validated text + vision configuration:

```text
1. verify text decode/correctness remains unchanged with vision loaded
2. enable MTP
3. benchmark correctness / acceptance / speed
4. enable DFlash
5. revisit smaller conventional tuning afterward
```
